
<% if @recipes.blank? %>
<p>There are not any recipes currently in the system.</p>
<% else %>
<p>These are the current recipes in our system</p>

<ul id = "recipes">
   <% @recipes.each do |c| %>
     <li>
	   <%= link_to c.idrecipes, {:action => "show", :id => c.id} -%><b>
	   <%= link_to 'Edit', {:action => "edit",      :id => c.id} %><b> 
	   <%= link_to "Delete", {:action => 'delete',  :id => c.id},
		:confirm => "Are you sure you want to delete this item?" %></b>
	 <li>
   <% end %>
</ul>

<% end %>
<p><%= link_to "Add new Recipe", {:action => 'new' }%></p>

